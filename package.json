{
  "name": "bootstrap-studio-terminal",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"cd client/ && yarn run start\" \"cd server/ && yarn run start\"",
    "init": "yarn install --no-lockfile"
  },
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.3.0",
    "jquery": "^3.5.1"
  }
}
